{
  "name": "diy-roomba-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ELEGOO Smart Robot Car を自作ルンバとして動かす Web アプリ（掃除ロジック＋シム＋Web Serial）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/w3c-web-serial": "^1.0.6",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
